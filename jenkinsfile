pipeline {
    agent any

        stages {
            stage('Clean Previous Containers') {
                steps {
                    sh 'docker-compose down || true'
                }
            }

        stage('Build Application') {
            steps {
                sh 'docker-compose build app'
            }
        }

       stage('Start Application') {
            steps {
                sh 'docker-compose up -d app'
                // Obtener la IP del contenedor de la aplicación
                sh '''
                    APP_IP=$(docker inspect -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}' sample_app4)
                    echo "IP de la aplicación: $APP_IP"
                    timeout 120s bash -c '
                    while ! curl -f http://$APP_IP:5050/login; do
                        sleep 5
                        echo "Esperando a que la aplicación esté lista..."
                    done
                    '
                '''
            }
        }

        stage('Run Cypress Tests') {
            steps {
                sh 'docker-compose run --rm cypress'
            }
        }

        stage('Cleanup') {
            steps {
                sh 'docker-compose down'
            }
        }
    }

    post {
        always {
            publishHTML target: [
                allowMissing: true,
                alwaysLinkToLastBuild: true,
                keepAll: true,
                reportDir: 'mochawesome-report',
                reportFiles: 'mochawesome.html',
                reportName: 'Cypress Test Report'
            ]
        }
    }
}